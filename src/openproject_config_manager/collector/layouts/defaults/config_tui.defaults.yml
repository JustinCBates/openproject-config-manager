# Configuration defaults for TUI layout
# Generated from discovery output via defaults mapping

defaults:
  domain: "openproject.local"
  port: "80"
  memory_limit: "2GB"
  database_setup: "container"
  ssl_configuration: "Self-signed certificate"
